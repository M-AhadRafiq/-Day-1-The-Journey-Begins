#include <iostream>
#include <vector>
#include <algorithm>
#include <ranges>
​int main() {
std::vector<int> numbers = {45, 12, 85, 32, 5};
​std::ranges::sort(numbers);
​for (int num : numbers | std::views::all) {
std::cout << num << " ";
}
return 0;
}
