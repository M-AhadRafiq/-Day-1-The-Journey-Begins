#include <iostream>

int main() {
    int numbers[5] = {10, 20, 30, 40, 50};

    // A for loop iterating through the array indices 0 to 4
    for (int i = 0; i < 5; i++) {
        std::cout << "Element at index " << i << ": " << numbers[i] << "\n";
    }

    return 0;
}
